#pragma once
#include <SFML/Graphics.hpp>


namespace game_setup
{
    const float W = 800.0f;
    const float H = 800.0f;
    const float blockSize = 25.0f;
    const float frameRate = 10.0f;


    const sf::Color color_1(95, 173, 86);                 // kolor węża
    const sf::Color color_2(77, 144, 120);                // kolor krawędzi węża

    const sf::Color color_3(242, 193, 78);                // kolor tła

    const sf::Color color_4(247, 129, 84);                //kolor jedzenia
    const sf::Color color_5(180, 67, 180);                //kolor krawędzi jedzenia


    // sf::Clock clk;

    // float deltaTime = 0.0f;
    // float totalTime = 0.0f;
    // const float moveSpeed = 50000.0f;


}
